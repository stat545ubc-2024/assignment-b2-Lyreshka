% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarizePlotR.R, R/summarize_and_plot.R
\name{summarize_and_plot}
\alias{summarize_and_plot}
\title{Summarize and plot function.}
\usage{
summarize_and_plot(
  data,
  x_var,
  y_vars,
  plot_types,
  y_var_colors = NULL,
  group_by = NULL,
  combined_plot = FALSE
)

summarize_and_plot(
  data,
  x_var,
  y_vars,
  plot_types,
  y_var_colors = NULL,
  group_by = NULL,
  combined_plot = FALSE
)
}
\arguments{
\item{data}{A data frame containing the variables to analyze.}

\item{x_var}{A string specifying the independent variable (categorical).}

\item{y_vars}{A character vector of dependent variables (numeric).}

\item{plot_types}{A character vector of plot types to create.}

\item{y_var_colors}{An optional named vector of colors for each y_var.}

\item{group_by}{An optional variable for grouping the summary.}

\item{combined_plot}{A boolean indicating if combined plots should be created.}
}
\value{
A list containing the summary table and generated plots.

A list containing the summary table and generated plots.
}
\description{
This function summarizes specified numeric variables in a dataset and
creates various types of plots (box, scatter, density, histogram, bar).

This function summarizes specified numeric variables in a dataset and
creates various types of plots (box, scatter, density, histogram, bar).
}
\examples{
x_var <- "cyl"
y_vars <- c("mpg", "hp", "wt")
plot_types <- c("box", "scatter", "density", "histogram", "bar")
custom_colors <- c(mpg = "purple", hp = "orange", wt = "green")
result <- summarize_and_plot(mtcars, x_var, y_vars, plot_types,
                                  y_var_colors = custom_colors, combined_plot = TRUE)
x_var <- "cyl"
y_vars <- c("mpg", "hp", "wt")
plot_types <- c("box", "scatter", "density", "histogram", "bar")
custom_colors <- c(mpg = "purple", hp = "orange", wt = "green")
result <- summarize_and_plot(mtcars, x_var, y_vars, plot_types,
                                  y_var_colors = custom_colors, combined_plot = TRUE)
}
